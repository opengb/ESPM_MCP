# Optional: path to the read-only CSV of ESPM accounts.
# Columns: username,password,env. Defaults to ./accounts.csv if unset.
# See accounts.csv.example for the format.
# ESPM_ACCOUNTS_CSV=./accounts.csv
